---
- name: 9k test
  hosts: all
  gather_facts: true
 connection: local

  vars:
    cli:
      username: ansible
      password: Co99erg0ld
      transport: cli

  tasks:
    - name: create access list
      nxos_config:
        provider: "{{ cli  }}"
        authorize: yes
        parents: ip access-list
        lines:
          - permit ip 192.168.0.0 0.0.255.255 any

        register: print_output

    - debug: var=print_output
